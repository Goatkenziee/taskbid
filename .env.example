# Clerk Auth (REQUIRED for __REDACTED_SECRET__set_in_env_not_source — set in Vercel env vars)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
CLERK_WEBHOOK_SECRET=

# Database (PostgreSQL via Prisma — set DATABASE_URL in Vercel)
DATABASE_URL=

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
